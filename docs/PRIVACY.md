# 隐私声明（班级工作台 ClassWorkBench）

**版本：v1.0.0**
**更新日期：2026 年 8 月 17 日**

---

## 1. 概述

"班级工作台"（以下简称"本软件"，英文名 ClassWorkBench）是一款运行于 Windows 的本地桌面应用，用于班级大屏展示作业、学科、天气等信息。

**我们秉持"隐私优先"原则：本软件无需注册、无账号体系、无云端同步，除下述明确说明的功能外，不收集、不上传、不共享您的任何个人数据。** 您在使用前请仔细阅读本声明，了解我们如何处理数据。

本软件以 **MIT 开源许可证**（MIT License）发布，源代码公开可审计——本声明所述的数据处理行为，均可通过公开的源代码进行验证。

## 2. 我们收集的信息

### 2.1 本地存储的数据（不离开您的设备）

以下数据**仅保存在您本机的应用数据目录**（userData），用于实现软件功能，不会发送到任何服务器：

| 数据 | 说明 | 存储位置 |
|---|---|---|
| 作业数据（homeworks） | 您录入的作业内容、科目、截止时间等 | 本机 userData 目录 |
| 学科配置（subjects） | 学科名称、颜色、图标等 | 本机 userData 目录 |
| 应用设置（settings） | 窗口布局、天气城市、配色方案、辅助功能开关、QQ 监听配置（含教师名单）等 | 本机 userData 目录 |
| 备份/快照文件 | 您主动导出的备份、自动生成的恢复快照 | 本机 userData 目录（restore-snapshots） |
| 日志文件 | electron-log 生成的本机运行日志 | 本机 userData 目录 |
| 背景图缓存（bg-cache） | 从第三方图源下载的风景图片，用于本地展示 | 本机 userData/bg-cache/ 目录 |

> 上述作业、学科、设置、快照等数据文件均经 **AES-256-GCM 加密**存储于本机（密钥由 Windows 凭据保护），技术细节见《数据的安全性》第 2 节。

> 打包安装版的数据目录通常为 `%APPDATA%\班级工作台`；开发模式运行时为 `%APPDATA%\classworkbench`。

### 2.2 我们不收集的信息

- **不收集**您的姓名、手机号、邮箱等个人身份信息；
- **不收集**使用行为统计、埋点、崩溃上报（日志仅写入本地文件）；
- **不进行**任何形式的远程遥测或数据回传。

## 3. QQ 通知监听功能（重要说明）

本软件的"QQ 捕获"功能会读取系统通知，请您特别注意：

- **用途**：开启后，本软件通过随附的辅助程序（qq-listener.exe）读取 Windows 系统通知，将 QQ 中与作业相关的消息展示在班级大屏上。
- **实现机制**：辅助程序调用 Windows 系统的 `UserNotificationListener` 接口读取系统 Toast 通知。首次使用需要您在 **"Windows 设置 → 隐私和安全性 → 通知 → 通知访问权限"** 中允许本软件访问通知。该权限由系统授予，仅用于读取通知内容。
- **过滤规则**：默认仅处理应用名为"QQ"的通知（可在设置中关闭此过滤）；配合去重与冷却机制，避免同一消息重复展示。
- **数据流向（关键）**：通知文本被读取后，**仅在本机内存中处理**（用于匹配教师名单、提取发送人和内容），然后**直接展示在您自己的屏幕上**。处理过程**不落盘、不上传、不转发给任何第三方**。辅助程序的内存去重记录会在程序退出后自动清空，定期清理，不会持久保存。
- **可随时关闭**：您可在设置面板关闭该功能，或在 Windows 系统通知权限设置中撤销授权。关闭后辅助程序停止运行，不再读取任何通知。

## 4. 第三方服务

以下功能在您**主动开启并使用**时，会将必要信息发送至第三方服务器：

| 功能 | 服务商 | 发送的数据 | 说明 |
|---|---|---|---|
| 天气查询 | 和风天气（QWeather） | 城市名、经纬度坐标 | 需要您自行配置 API Key（保存在本地） |
| 天气查询 | Open-Meteo | 经纬度坐标 | 开源天气服务，无需 Key |
| 背景图下载 | Upx8 / XXAPI | 仅请求图片资源，不发送个人信息 | 下载的图片缓存在本机 |

第三方服务由相应服务商独立运营，其数据收集与使用行为受其自身隐私政策的约束。**我们建议您访问相应服务商官网查阅其隐私政策。**

> 背景图片会缓存在您的本机 `userData/bg-cache/` 目录用于本地展示，其版权与授权说明请见 `THIRD-PARTY-LICENSES` 文件。

> 说明：本软件不会将您的作业内容、QQ 通知内容或任何本地数据发送给上述服务商。

此外，本软件界面图标含字节跳动（ByteDance）开源的 **IconPark** 图标库（Apache License 2.0），为打包内置的静态资源，不涉及网络请求。许可与署名详见《用户协议》及仓库根目录 `THIRD-PARTY-LICENSES` 文件。

## 5. 数据安全与保留

- 本软件数据仅存本地，不经过网络传输（除第 4 条列明的天气/背景图查询外）；
- 您在设置中录入的 API Key 仅保存在本机配置文件，不上传至任何服务器。

## 6. 数据删除

- 您可随时在软件内删除作业、学科数据；
- 卸载本软件**不会自动删除**本机数据目录，如需彻底清除全部数据，请手动删除上述 userData 目录；
- 删除前建议先使用软件内置的备份功能导出，数据一旦删除即永久丢失。

## 7. 未成年人信息与班级场景提示

本软件面向班级教学场景，您录入的作业数据可能涉及未成年学生信息。请注意：

- 本软件**不主动收集**学生的个人信息，相关内容仅是您在本机自行录入的数据；
- 建议学校、教师严格遵守《中华人民共和国个人信息保护法》《未成年人网络保护条例》等法律法规，对班级大屏展示的内容进行必要审核，避免展示敏感信息；
- 在公共大屏使用时，请勿录入或展示学生手机号、身份证号、家庭住址等敏感个人信息。

## 8. 政策的更新

如本软件的数据处理方式发生重大变化，我们将更新本声明，并在新版本发布时生效。更新后的声明将随软件一同分发。您在声明更新后继续使用本软件，视为接受更新后的内容。

## 9. 联系我们

如您对本声明或本软件的数据处理有任何疑问、意见或建议，请通过软件分发页面、软件内置帮助入口或开源仓库与我们联系，具体渠道见独立文档《联系我们》。我们将在合理期限内予以回复。

---

*本声明旨在帮助您了解本软件如何处理数据。请放心使用，您的数据属于您自己。*
