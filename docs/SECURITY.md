# 数据的安全性（班级工作台 ClassWorkBench）

**版本：v1.0.1**
**更新日期：2026 年 8 月 21 日**

---

## 1. 概述

"班级工作台"（以下简称"本软件"）是一款基于 Electron 的本地桌面应用。我们在设计上贯彻**"本地优先、最小联网、最小权限"**原则：您的数据默认只存在于您自己的设备上，软件本身不内置任何账号体系、云同步、远程遥测或数据回传通道。**仅有的后台网络行为**是：在启动时向开发者自有渠道发起"下载"，自动获取最新版《用户协议》等法律文档，并静默检查是否有新版本可更新（详见第 4 条）；这些操作均为单向下载，不会上传任何本地数据。

本文档说明本软件为保护您的数据所采取的技术措施，以及已知的限制与建议。关于"我们处理哪些数据"，请参阅《隐私声明》；关于"使用许可与责任"，请参阅《用户协议》。

本软件以 **MIT 开源许可证**（MIT License）发布，源代码公开可审计——本文档所述的安全机制（原子写入、进程隔离、白名单 API 等）均可在源码中验证。

## 2. 数据存储安全

### 2.1 本地加密存储，无服务器中转

作业数据、学科配置、应用设置、归档与恢复快照均**加密后**保存在本机应用数据目录（`%APPDATA%\classworkbench`，打包版为 `%APPDATA%\班级工作台`）：

- **加密算法**：AES-256-GCM，密文格式 `CBW1:<iv>:<tag>:<data>`，直接打开数据文件为乱码；
- **密钥保护**：32 字节随机密钥经 Windows 凭据保护（DPAPI）加密后落盘（`.cbw-key`），只有当前 Windows 用户可解；
- **覆盖范围**：主数据 `homework-data.enc`、归档 `archives/*.json`、恢复快照 `restore-snapshots/*.json` 均为密文；背景图缓存等公开资源不加密；
- **自动迁移**：旧版本明文 `homework-data.json` 在首次启动时自动迁移为加密文件，明文文件改名保留；
- **损坏自愈**：密文被篡改或损坏时，GCM 认证失败 → 备份损坏文件后回退默认值，不丢失现场、不崩溃；
- 本软件**不架设任何后端服务器接收或存储您的数据**，除下文第 4 条说明的天气/背景图查询、法律文档在线下载与软件自动更新下载外，数据不离开您的设备。

### 2.2 原子写入，防止文件损坏

写入作业、备份、归档等数据时，本软件采用**原子写入**策略（`safeWriteJson`）：

1. 先将完整内容写入一个临时文件（`<目标文件>.tmp-<时间戳>`）；
2. 写入成功后，通过文件系统 `rename` 操作原子性地替换目标文件。

这样即使写入过程中发生断电、崩溃或磁盘异常，目标文件也只会是"旧完整版本"或"新完整版本"之一，**不会出现写一半的损坏文件**。

### 2.3 归档路径校验，防止路径穿越

按月归档文件按 `YYYY-MM.json` 命名。恢复归档时，本软件对月份键进行**正则白名单校验**（仅允许 `\d{4}-\d{2}` 格式），杜绝通过构造非法月份键访问任意文件路径的风险。

## 3. 数据备份与恢复安全

### 3.1 恢复前自动快照

当您执行"恢复"操作时，本软件会**先用当前数据生成一份快照**（写入 `userData/restore-snapshots/restore-<时间戳>.json`），再覆盖现有数据。这意味着即使恢复操作有误，您仍可从快照中找回恢复前的状态。

### 3.2 导入格式校验

导入备份文件时，本软件会校验：

- 文件能被正确解析为 JSON；
- 顶层是对象而非数组或标量；
- 归档数据按月份键白名单校验、按作业 `id` 去重合并。

格式不合法的文件会被拒绝并提示，不会写入破坏性数据。

### 3.3 用户掌控存储位置

导出备份通过系统保存对话框由您**自选存储路径**（默认文件名"班级工作台备份.json"），备份文件存到哪里完全由您决定，本软件不会将备份静默发送到任何位置。

## 4. 数据传输边界

本软件在以下情况会向第三方发起网络请求，且仅传输完成功能所需的最小信息：

| 场景 | 传输内容 | 目的地 | 是否含您的作业数据 |
|---|---|---|---|
| 天气查询 | 城市名、经纬度坐标 | 和风天气 / Open-Meteo | 否 |
| 背景图下载 | 仅请求图片资源 URL | Upx8 / XXAPI 壁纸站 | 否 |
| 法律文档在线同步 | 仅发起"下载"请求，获取最新版协议文档 | 开发者自有渠道（GitHub Pages / CDN / GitHub 仓库） | 否 |
| 软件自动更新 | 更新元数据（版本号、平台）与更新安装包下载 | GitHub Releases（开发者发布渠道） | 否 |

除上述场景外，本软件不向任何地址发送您的作业内容、学科配置、设置项或备份文件。其中，法律文档在线同步为**单向下载**：仅从开发者自有渠道拉取文档文本，**不上传、不发送任何本地数据**；该文档获取后仅在您本机缓存与展示。

## 5. 进程隔离与最小权限

### 5.1 Electron 安全基线

主窗口严格遵循 Electron 官方推荐的安全配置：

| 配置项 | 值 | 作用 |
|---|---|---|
| `contextIsolation` | `true` | 预加载脚本与渲染层运行环境隔离 |
| `nodeIntegration` | `false` | 渲染层无 Node.js 访问权，无法直接读写文件系统 |
| `sandbox` | `true` | 渲染进程沙箱化，限制系统能力 |
| `webgl` | `false` | 关闭 WebGL，减少攻击面 |
| `spellcheck` | `false` | 关闭拼写检查（避免向拼写服务发送输入） |

### 5.2 白名单 API 暴露

渲染层与主进程之间仅通过预加载脚本（`preload.js`、`floating-preload.js`）用 `contextBridge` 暴露的**白名单 API** 通信。渲染层无法直接调用任意 Node 接口或 IPC 通道，只能调用预先定义、经过主进程校验的方法。

### 5.3 Sidecar 最小配置

启动 QQ 监听辅助程序（`qq-listener.exe`）时，本软件仅向其传递**完成功能所需的最小配置**：

- 扫描间隔、冷却时长、是否只处理 QQ；
- 教师名单**仅传昵称字符串**，不传任何密码、Token 或账号凭据。

配置通过一个临时文件传递，辅助程序退出后**该临时文件会被立即删除**，不在磁盘留存。

## 6. QQ 通知捕获的安全措施

该功能的数据处理行为（读取范围、仅内存匹配、不上传、可关闭等）详见《隐私声明》第 3 条。本文档仅说明相关的安全技术措施：

- **读取范围受控**：默认仅处理应用名为"QQ"的通知（可配置），其他应用通知被忽略；
- **不持久化**：去重用的哈希字典仅存于内存，程序退出即清空，并定期清理过期条目（超过 1000 条时触发清理），不会无限增长；
- **最小权限**：Sidecar 仅接收完成功能所需的最小配置（见第 5.3 条），临时配置文件退出即删。

## 7. 异常与崩溃保护

### 7.1 Sidecar 崩溃自愈

辅助程序异常退出时，主进程采用**指数退避**策略自动重启（间隔 3 → 6 → 12 → 24 … → 384 秒），避免反复崩溃拖垮系统。连续崩溃达到上限（8 次）后**停止自动重启**并提示用户检查，不会陷入死循环。

### 7.2 输出缓冲上限

辅助程序的标准输出解析设有 **1MB 单行缓冲上限**，防止异常超长输出导致内存无限增长。

### 7.3 背景图完整性校验

下载的背景图在缓存前会进行**文件完整性校验**：检测 JPEG / PNG / WebP / GIF 的魔数头与尾标，防止损坏或伪装的图片进入缓存。同时设有限制：单张不超过 20MB、缓存总数不超过 6 张、下载超时 30 秒。

### 7.4 渲染进程监控

主进程监听 `render-process-gone`、`did-fail-load` 等事件并记录到本地日志，便于排查异常，不向外部上报。

## 8. 临时数据与日志

- **本地日志**：本软件使用 `electron-log` 将运行日志写入本机 `userData/logs/` 目录，**仅用于本地排查，不上传**；

Sidecar 的临时配置文件与内存去重记录的清理机制见第 5.3 条与第 6 条。

## 9. 数据删除与可控性

您的数据完全由您掌控：可在软件内随时删除作业、学科；卸载本软件不会自动删除数据目录（防止误卸载导致数据丢失）。删除与备份的具体指引详见《隐私声明》第 6 条。

## 10. 第三方依赖

本软件运行时依赖以下库，均来自 npm 官方仓库：

| 依赖 | 用途 | 安全相关说明 |
|---|---|---|
| `atomically` | 原子文件写入 | 保证写入完整性 |
| `electron-log` | 本地日志 | 仅写本地文件 |
| `electron-updater` | 自动更新 | 仅访问开发者自己的 GitHub 发布渠道获取更新，不涉及本地数据 |

本软件**不集成**任何用户行为分析 SDK、广告 SDK、崩溃上报 SDK 或远程配置下发组件。

此外，本软件界面图标含字节跳动（ByteDance）开源的 **IconPark** 图标库（Apache License 2.0），为打包内置的静态资源，不参与运行时网络请求。许可与署名详见《用户协议》及仓库根目录 `THIRD-PARTY-LICENSES` 文件。

## 11. 已知限制与使用建议

为帮助您做出知情判断，本软件存在以下已知限制：

1. **加密边界**：数据加密防止的是"同权限的恶意软件扫描磁盘读取数据文件"；**无法防止**针对进程的内存注入读取（数据在内存中为明文），也无法防止拥有系统级权限（管理员/内核）或专门针对本机的攻击者。建议为登录账户设置强密码，并避免与他人共享账户。
2. **备份文件未加密**：导出的备份 JSON 文件不含加密，请妥善保管，避免存放在公共可访问位置。
3. **大屏可见性**：在教室大屏使用时，屏幕内容可能被多人看到，请避免录入学生手机号、身份证号等敏感个人信息。
4. **第三方服务独立担责**：天气、背景图等第三方服务的数据安全由相应服务商负责，建议关注其安全公告。

## 12. 安全措施的未来更新

如本软件的数据安全措施发生重大变化（如新增加密、新增功能涉及新的数据处理），我们将更新本文档，并在新版本发布时生效。

## 13. 联系我们

如您对本软件的数据安全措施有任何疑问、发现安全漏洞或有改进建议，请通过软件分发页面、软件内置帮助入口或开源仓库与我们联系，具体渠道见独立文档《联系我们》。我们重视每一位用户的安全反馈。

---

*本软件致力于以最小化的数据流转，为您提供安全可控的班级教学工具。*
